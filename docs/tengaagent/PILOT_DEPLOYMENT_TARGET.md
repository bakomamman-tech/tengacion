# TengaAgent Pilot Deployment Target

Use a full-stack preview/pilot Render service backed by `feature/tengaagent-lead-capture-multitenant`. The existing frontend-only preview is suitable for visual inspection but cannot validate database, billing, AI, WhatsApp, voice-note or authenticated owner behavior without a backend runtime and environment configuration.
