# TengaAgent Pilot Readiness Response Contract

The authenticated owner readiness endpoint returns tenant-scoped readiness metadata, channel readiness booleans, integration counts/statuses, billing summary and non-secret environment readiness booleans. It must never return raw environment-variable values.
