# TengaAgent Pilot Configuration Handoff

At the Render configuration handoff, provide variable names, purpose, source system and whether each is required for web, WhatsApp, voice or calendar integrations. Do not ask the user to send credential values in chat when they can enter them directly into Render.
