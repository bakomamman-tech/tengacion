# TengaAgent Pilot Readiness API

Authenticated owner endpoint:

`GET /api/tengaagent/owner/pilot-readiness`

The route resolves the owner's workspace server-side and returns readiness for that organization only. It is intended for the owner dashboard and controlled pilot diagnostics.
